;; == external FFI ==

(load "ffi.lisp")

;; == Common Lisp ==

; hoisted functions

(defun |fn1001| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|Γ| (cdr |payload|)))
      (let ((|_pL#x| (svref (cdr |Γ|) 0)))
        (let ((|_pL#k| (car |α|)))
          (let ((|u2| nil))
            (let ((|pair3| (cons |_pL#x| |u2|)))
              (let ((|_code| (svref (cdr |_pL#k|) 0)))
                (let ((|Γc| (svref (cdr |_pL#k|) 1)))
                  (let ((|ρc| (cons |pair3| |Γc|)))
                    (funcall |_code| |ρc| |k|)))))))))))

(defun |fn1000| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|_pL#x| (car |α|)))
      (let ((|Γ| (cons '|𝐄| (vector |_pL#x|))))
        (let ((|lam5| (cons '|𝐂| (vector #'|fn1001| |Γ|))))
          (let ((|con6| (cons '|Cont| (vector |lam5|))))
            (funcall |k| |con6|)))))))

(defun |fn1002| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|_pL#c| (car |α|)))
      (let ((|_pR#c| (cdr |α|)))
        (cond
          ((eq (car |_pL#c|) '|Cont|)
            (let ((|p10| (svref (cdr |_pL#c|) 0)))
              (let ((|u11| nil))
                (let ((|pair12| (cons |_pR#c| |u11|)))
                  (let ((|_code| (svref (cdr |p10|) 0)))
                    (let ((|Γc| (svref (cdr |p10|) 1)))
                      (let ((|ρc| (cons |pair12| |Γc|)))
                        (funcall |_code| |ρc| |k|)))))))))))))

(defun |fn1005| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|Γ| (cdr |payload|)))
      (let ((|_pR#__?x₀| (svref (cdr |Γ|) 1)))
        (let ((|_pL#k| (svref (cdr |Γ|) 0)))
          (let ((|_pL#a| (car |α|)))
            (let ((|u20| nil))
              (let ((|pair21| (cons |_pL#a| |u20|)))
                (let ((|_code| (svref (cdr |_pR#__?x₀|) 0)))
                  (let ((|Γc| (svref (cdr |_pR#__?x₀|) 1)))
                    (let ((|ρc| (cons |pair21| |Γc|)))
                      (labels ((|k1| (|v0|)
                        (cond
                          ((eq (car |v0|) '|Cont|)
                            (let ((|p24| (svref (cdr |v0|) 0)))
                              (let ((|u25| nil))
                                (let ((|pair26| (cons |_pL#k| |u25|)))
                                  (let ((|_code| (svref (cdr |p24|) 0)))
                                    (let ((|Γc| (svref (cdr |p24|) 1)))
                                      (let ((|ρc| (cons |pair26| |Γc|)))
                                        (funcall |_code| |ρc| |k|)))))))))))
                        (funcall |_code| |ρc| #'|k1|)))))))))))))

(defun |fn1004| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|Γ| (cdr |payload|)))
      (let ((|p17| (svref (cdr |Γ|) 1)))
        (let ((|_pR#__?x₀| (svref (cdr |Γ|) 0)))
          (let ((|_pL#k| (car |α|)))
            (let ((|Γ| (cons '|𝐄| (vector |_pL#k| |_pR#__?x₀|))))
              (let ((|lam28| (cons '|𝐂| (vector #'|fn1005| |Γ|))))
                (let ((|u29| nil))
                  (let ((|pair30| (cons |lam28| |u29|)))
                    (let ((|_code| (svref (cdr |p17|) 0)))
                      (let ((|Γc| (svref (cdr |p17|) 1)))
                        (let ((|ρc| (cons |pair30| |Γc|)))
                          (funcall |_code| |ρc| |k|))))))))))))))

(defun |fn1003| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|_pL#__?x₀| (car |α|)))
      (let ((|_pR#__?x₀| (cdr |α|)))
        (cond
          ((eq (car |_pL#__?x₀|) '|Cont|)
            (let ((|p17| (svref (cdr |_pL#__?x₀|) 0)))
              (let ((|Γ| (cons '|𝐄| (vector |_pR#__?x₀| |p17|))))
                (let ((|lam32| (cons '|𝐂| (vector #'|fn1004| |Γ|))))
                  (let ((|con33| (cons '|Cont| (vector |lam32|))))
                    (funcall |k| |con33|)))))))))))

(defun |fn1009| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|Γ| (cdr |payload|)))
    (let ((|_pL#k| (svref (cdr |Γ|) 1)))
      (let ((|_pL#a| (svref (cdr |Γ|) 0)))
        (let ((|u39| nil))
          (let ((|pair40| (cons |_pL#a| |u39|)))
            (let ((|_code| (svref (cdr |_pL#k|) 0)))
              (let ((|Γc| (svref (cdr |_pL#k|) 1)))
                (let ((|ρc| (cons |pair40| |Γc|)))
                  (funcall |_code| |ρc| |k|))))))))))

(defun |fn1008| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|Γ| (cdr |payload|)))
      (let ((|_pL#k| (svref (cdr |Γ|) 0)))
        (let ((|_pL#a| (car |α|)))
          (let ((|Γ| (cons '|𝐄| (vector |_pL#a| |_pL#k|))))
            (let ((|lam42| (cons '|𝐂| (vector #'|fn1009| |Γ|))))
              (let ((|con43| (cons '|Cont| (vector |lam42|))))
                (funcall |k| |con43|)))))))))

(defun |fn1007| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|Γ| (cdr |payload|)))
      (let ((|_pL#f| (svref (cdr |Γ|) 0)))
        (let ((|_pL#k| (car |α|)))
          (let ((|Γ| (cons '|𝐄| (vector |_pL#k|))))
            (let ((|lam44| (cons '|𝐂| (vector #'|fn1008| |Γ|))))
              (let ((|u45| nil))
                (let ((|pair46| (cons |lam44| |u45|)))
                  (let ((|_code| (svref (cdr |_pL#f|) 0)))
                    (let ((|Γc| (svref (cdr |_pL#f|) 1)))
                      (let ((|ρc| (cons |pair46| |Γc|)))
                        (labels ((|k3| (|v2|)
                          (cond
                            ((eq (car |v2|) '|Cont|)
                              (let ((|p49| (svref (cdr |v2|) 0)))
                                (let ((|u50| nil))
                                  (let ((|pair51| (cons |_pL#k| |u50|)))
                                    (let ((|_code| (svref (cdr |p49|) 0)))
                                      (let ((|Γc| (svref (cdr |p49|) 1)))
                                        (let ((|ρc| (cons |pair51| |Γc|)))
                                          (funcall |_code| |ρc| |k|)))))))))))
                          (funcall |_code| |ρc| #'|k3|))))))))))))))

(defun |fn1006| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|_pL#f| (car |α|)))
      (let ((|Γ| (cons '|𝐄| (vector |_pL#f|))))
        (let ((|lam53| (cons '|𝐂| (vector #'|fn1007| |Γ|))))
          (let ((|con54| (cons '|Cont| (vector |lam53|))))
            (funcall |k| |con54|)))))))

(defun |fn1011| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|Γ| (cdr |payload|)))
    (let ((|_pL#k| (svref (cdr |Γ|) 0)))
      (let ((|c62| 42))
        (let ((|u63| nil))
          (let ((|pair64| (cons |c62| |u63|)))
            (let ((|_code| (svref (cdr |_pL#k|) 0)))
              (let ((|Γc| (svref (cdr |_pL#k|) 1)))
                (let ((|ρc| (cons |pair64| |Γc|)))
                  (funcall |_code| |ρc| |k|))))))))))

(defun |fn1010| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|Γ| (cdr |payload|)))
      (let ((|lam7| (svref (cdr |Γ|) 1)))
        (let ((|lam34| (svref (cdr |Γ|) 0)))
          (let ((|_pL#k| (car |α|)))
            (let ((|c57| 0))
              (let ((|u58| nil))
                (let ((|pair59| (cons |c57| |u58|)))
                  (let ((|_code| (svref (cdr |lam7|) 0)))
                    (let ((|Γc| (svref (cdr |lam7|) 1)))
                      (let ((|ρc| (cons |pair59| |Γc|)))
                        (labels ((|k5| (|v4|)
                          (let ((|Γ| (cons '|𝐄| (vector |_pL#k|))))
                            (let ((|lam66| (cons '|𝐂| (vector #'|fn1011| |Γ|))))
                              (let ((|pair67| (cons |v4| |lam66|)))
                                (let ((|_code| (svref (cdr |lam34|) 0)))
                                  (let ((|Γc| (svref (cdr |lam34|) 1)))
                                    (let ((|ρc| (cons |pair67| |Γc|)))
                                      (funcall |_code| |ρc| |k|)))))))))
                          (funcall |_code| |ρc| #'|k5|))))))))))))))

(defun |fn1015| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|_pL#x| (car |α|)))
      (funcall |k| |_pL#x|))))

; entrypoint
(defun |main| (|arg| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|Γ| (cons '|𝐄| (vector))))
    (let ((|lam7| (cons '|𝐂| (vector #'|fn1000| |Γ|))))
      (let ((|Γ| (cons '|𝐄| (vector))))
        (let ((|lam14| (cons '|𝐂| (vector #'|fn1002| |Γ|))))
          (let ((|Γ| (cons '|𝐄| (vector))))
            (let ((|lam34| (cons '|𝐂| (vector #'|fn1003| |Γ|))))
              (let ((|Γ| (cons '|𝐄| (vector |lam34| |lam7|))))
                (let ((|lam69| (cons '|𝐂| (vector #'|fn1010| |Γ|))))
                  (let ((|u70| nil))
                    (let ((|pair71| (cons |lam69| |u70|)))
                      (let ((|ρc1014| (cons |pair71| |Γ|)))
                        (labels ((|k7| (|v6|)
                          (let ((|Γ| (cons '|𝐄| (vector))))
                            (let ((|lam74| (cons '|𝐂| (vector #'|fn1015| |Γ|))))
                              (let ((|pair75| (cons |v6| |lam74|)))
                                (let ((|_code| (svref (cdr |lam14|) 0)))
                                  (let ((|Γc| (svref (cdr |lam14|) 1)))
                                    (let ((|ρc| (cons |pair75| |Γc|)))
                                      (funcall |_code| |ρc| |k|)))))))))
                          (funcall #'|fn1006| |ρc1014| #'|k7|))))))))))))))

; driver
(defun |__start| ()
  (format t "~A"
    (funcall #'|main| nil #'identity)))

