;; == Runtime ==
(load "runtime.lisp")

;; == Linked Lisp Source ==
(load "ffi.lisp")

;; == Common Lisp ==

; hoisted functions

(defun |fn1003| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)) (ignorable |payload|))
  (let ((|α1004| (car |payload|)))
  (let ((|_pL#x| (car |α1004|)))
  (let ((|con1| (cons '|Some| (vector |_pL#x|))))
  (funcall (the function |k|) |con1|)))))

(defun |fn1006| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)) (ignorable |payload|))
  (let ((|α1007| (car |payload|)))
  (let ((|_pL#x| (car |α1007|)))
  (let ((|_pR#x| (cdr |α1007|)))
  (cond
    ((eq (car |_pL#x|) '|None|)
      (let ((|con4| (cons '|None| (vector))))
      (funcall (the function |k|) |con4|)))
    ((eq (car |_pL#x|) '|Some|)
      (let ((|p5| (svref (cdr |_pL#x|) 0)))
      (let ((|u6| nil))
      (let ((|pair7| (cons |p5| |u6|)))
      (let ((|_code1009| (svref (cdr |_pR#x|) 0)))
      (let ((|Γc1010| (svref (cdr |_pR#x|) 1)))
      (let ((|ρc1011| (cons |pair7| |Γc1010|)))
      (funcall (the function |_code1009|) |ρc1011| |k|)))))))))))))

(defun |fn1018| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)) (ignorable |payload|))
  (let ((|α1019| (car |payload|)))
  (let ((|Γ1020| (cdr |payload|)))
  (let ((|lam2| (svref (cdr |Γ1020|) 2)))
  (let ((|_pL#y| (svref (cdr |Γ1020|) 1)))
  (let ((|_pL#x| (svref (cdr |Γ1020|) 0)))
  (let ((|_pL#z| (car |α1019|)))
  (let ((|p29| (%int+ |_pL#x| |_pL#y|)))
  (let ((|p30| (%int+ |p29| |_pL#z|)))
  (let ((|u31| nil))
  (let ((|pair32| (cons |p30| |u31|)))
  (let ((|_code1021| (svref (cdr |lam2|) 0)))
  (let ((|Γc1022| (svref (cdr |lam2|) 1)))
  (let ((|ρc1023| (cons |pair32| |Γc1022|)))
  (funcall (the function |_code1021|) |ρc1023| |k|)))))))))))))))

(defun |fn1015| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)) (ignorable |payload|))
  (let ((|α1016| (car |payload|)))
  (let ((|Γ1017| (cdr |payload|)))
  (let ((|lam9| (svref (cdr |Γ1017|) 2)))
  (let ((|lam2| (svref (cdr |Γ1017|) 1)))
  (let ((|_pL#x| (svref (cdr |Γ1017|) 0)))
  (let ((|_pL#y| (car |α1016|)))
  (let ((|c23| 30))
  (let ((|u24| nil))
  (let ((|pair25| (cons |c23| |u24|)))
  (let ((|_code1028| (svref (cdr |lam2|) 0)))
  (let ((|Γc1029| (svref (cdr |lam2|) 1)))
  (let ((|ρc1030| (cons |pair25| |Γc1029|)))
  (labels
    ((|k1| (|v0|)
      (let ((|Γ1027| (cons '|𝐄| (vector |_pL#x| |_pL#y| |lam2|))))
      (let ((|lam34| (cons '|𝐂| (vector #'|fn1018| |Γ1027|))))
      (let ((|pair35| (cons |v0| |lam34|)))
      (let ((|_code1024| (svref (cdr |lam9|) 0)))
      (let ((|Γc1025| (svref (cdr |lam9|) 1)))
      (let ((|ρc1026| (cons |pair35| |Γc1025|)))
      (funcall (the function |_code1024|) |ρc1026| |k|)))))))))
    (funcall (the function |_code1028|) |ρc1030| #'|k1|)))))))))))))))

(defun |fn1012| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)) (ignorable |payload|))
  (let ((|α1013| (car |payload|)))
  (let ((|Γ1014| (cdr |payload|)))
  (let ((|lam9| (svref (cdr |Γ1014|) 1)))
  (let ((|lam2| (svref (cdr |Γ1014|) 0)))
  (let ((|_pL#x| (car |α1013|)))
  (let ((|con19| (cons '|None| (vector))))
  (let ((|Γ1034| (cons '|𝐄| (vector |_pL#x| |lam2| |lam9|))))
  (let ((|lam37| (cons '|𝐂| (vector #'|fn1015| |Γ1034|))))
  (let ((|pair38| (cons |con19| |lam37|)))
  (let ((|_code1031| (svref (cdr |lam9|) 0)))
  (let ((|Γc1032| (svref (cdr |lam9|) 1)))
  (let ((|ρc1033| (cons |pair38| |Γc1032|)))
  (funcall (the function |_code1031|) |ρc1033| |k|))))))))))))))

; entrypoint
(defun |main| (|payload1000| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)) (ignorable |payload1000|))
  (let ((|Γ1043| (cons '|𝐄| (vector))))
  (let ((|lam2| (cons '|𝐂| (vector #'|fn1003| |Γ1043|))))
  (let ((|Γ1042| (cons '|𝐄| (vector))))
  (let ((|lam9| (cons '|𝐂| (vector #'|fn1006| |Γ1042|))))
  (let ((|c13| 20))
  (let ((|u14| nil))
  (let ((|pair15| (cons |c13| |u14|)))
  (let ((|ρc1041| (cons |pair15| |Γ1043|)))
  (labels
    ((|k3| (|v2|)
      (let ((|Γ1038| (cons '|𝐄| (vector |lam2| |lam9|))))
      (let ((|lam40| (cons '|𝐂| (vector #'|fn1012| |Γ1038|))))
      (let ((|pair41| (cons |v2| |lam40|)))
      (let ((|_code1035| (svref (cdr |lam9|) 0)))
      (let ((|Γc1036| (svref (cdr |lam9|) 1)))
      (let ((|ρc1037| (cons |pair41| |Γc1036|)))
      (funcall (the function |_code1035|) |ρc1037| |k|)))))))))
    (funcall #'|fn1003| |ρc1041| #'|k3|)))))))))))

; driver
(defun |__start| ()
  (format t "~A"
    (funcall #'|main| nil #'identity)))

