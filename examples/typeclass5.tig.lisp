;; == external FFI ==

(load "ffi.lisp")

;; == Common Lisp ==

; hoisted functions

(defun |fn1000| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|_pL#f| (car |α|)))
      (let ((|_pR#f| (cdr |α|)))
        (cond
          ((eq (car |_pR#f|) '|None|)
            (let ((|con2| (cons '|None| (vector))))
              (funcall |k| |con2|)))
          ((eq (car |_pR#f|) '|Some|)
            (let ((|p3| (svref (cdr |_pR#f|) 0)))
              (let ((|u4| nil))
                (let ((|pair5| (cons |p3| |u4|)))
                  (let ((|_code1001| (svref (cdr |_pL#f|) 0)))
                    (let ((|Γc1002| (svref (cdr |_pL#f|) 1)))
                      (let ((|ρc1003| (cons |pair5| |Γc1002|)))
                        (labels ((|k1| (|v0|)
                          (let ((|con7| (cons '|Some| (vector |v0|))))
                            (funcall |k| |con7|))))
                          (funcall |_code1001| |ρc1003| #'|k1|))))))))))))))

(defun |listMap| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|Γ| (cdr |payload|)))
      (let ((|_pL#f| (car |α|)))
        (let ((|_pR#f| (cdr |α|)))
          (cond
            ((eq (car |_pR#f|) '|Nil|)
              (let ((|con12| (cons '|Nil| (vector))))
                (funcall |k| |con12|)))
            ((eq (car |_pR#f|) '|Cons|)
              (let ((|p13| (svref (cdr |_pR#f|) 0)))
                (let ((|p14| (svref (cdr |_pR#f|) 1)))
                  (let ((|u15| nil))
                    (let ((|pair16| (cons |p13| |u15|)))
                      (let ((|_code1005| (svref (cdr |_pL#f|) 0)))
                        (let ((|Γc1006| (svref (cdr |_pL#f|) 1)))
                          (let ((|ρc1007| (cons |pair16| |Γc1006|)))
                            (labels ((|k3| (|v2|)
                              (let ((|pair18| (cons |_pL#f| |p14|)))
                                (let ((|ρ1004| (cons |pair18| |Γ|)))
                                  (labels ((|k5| (|v4|)
                                    (let ((|con20| (cons '|Cons| (vector |v2| |v4|))))
                                      (funcall |k| |con20|))))
                                    (funcall #'|listMap| |ρ1004| #'|k5|))))))
                              (funcall |_code1005| |ρc1007| #'|k3|))))))))))))))))

(defun |fn1008| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|_pL#x| (car |α|)))
      (funcall |k| |_pL#x|))))

(defun |fn1009| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|_pL#?x₀| (car |α|)))
      (let ((|c26| 2))
        (let ((|p27| (+ |c26| |_pL#?x₀|)))
          (funcall |k| |p27|))))))

(defun |fn1013| (|payload| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|α| (car |payload|)))
    (let ((|Γ| (cdr |payload|)))
      (let ((|lam23| (svref (cdr |Γ|) 1)))
        (let ((|c34| (svref (cdr |Γ|) 0)))
          (let ((|_pL#arg36| (car |α|)))
            (let ((|pair39| (cons |c34| |_pL#arg36|)))
              (let ((|_code1014| (svref (cdr |lam23|) 0)))
                (let ((|Γc1015| (svref (cdr |lam23|) 1)))
                  (let ((|ρc1016| (cons |pair39| |Γc1015|)))
                    (funcall |_code1014| |ρc1016| |k|)))))))))))

; entrypoint
(defun |main| (|arg| |k|)
  (declare (optimize (speed 3) (safety 0) (debug 0)))
  (let ((|Γ| (cons '|𝐄| (vector))))
    (let ((|listMap| (cons '|𝐂| (vector #'|listMap| |Γ|))))
      (let ((|Γ| (cons '|𝐄| (vector))))
        (let ((|lam23| (cons '|𝐂| (vector #'|fn1008| |Γ|))))
          (let ((|Γ| (cons '|𝐄| (vector))))
            (let ((|lam28| (cons '|𝐂| (vector #'|fn1009| |Γ|))))
              (let ((|c29| 1))
                (let ((|con30| (cons '|Some| (vector |c29|))))
                  (let ((|pair31| (cons |lam28| |con30|)))
                    (let ((|ρc1012| (cons |pair31| |Γ|)))
                      (labels ((|k7| (|v6|)
                        (let ((|c34| t))
                          (let ((|Γ| (cons '|𝐄| (vector |c34| |lam23|))))
                            (let ((|clos40| (cons '|𝐂| (vector #'|fn1013| |Γ|))))
                              (let ((|c41| 1))
                                (let ((|c42| 2))
                                  (let ((|c43| 3))
                                    (let ((|con44| (cons '|Nil| (vector))))
                                      (let ((|con45| (cons '|Cons| (vector |c43| |con44|))))
                                        (let ((|con46| (cons '|Cons| (vector |c42| |con45|))))
                                          (let ((|con47| (cons '|Cons| (vector |c41| |con46|))))
                                            (let ((|pair48| (cons |clos40| |con47|)))
                                              (let ((|ρc1019| (cons |pair48| |Γ|)))
                                                (labels ((|k9| (|v8|)
                                                  (let ((|p50| (cons |v6| |v8|)))
                                                    (funcall |k| |p50|))))
                                                  (funcall #'|listMap| |ρc1019| #'|k9|))))))))))))))))
                        (funcall #'|fn1000| |ρc1012| #'|k7|)))))))))))))

; driver
(defun |__start| ()
  (format t "~A"
    (funcall #'|main| nil #'identity)))

